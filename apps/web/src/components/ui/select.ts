/** Shared native-<select> styling. Compose per use (e.g. `cn(selectCls, 'h-7')`, or append
 *  `font-mono`) rather than re-declaring the string. */
export const selectCls =
  'h-8 rounded-md border border-border bg-bg px-2 text-[13px] text-text outline-none focus:border-brand disabled:opacity-50';
